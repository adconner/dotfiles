alias vi="nvim"
alias v="nvim"
alias r="ranger-cd"
alias :q="exit"
alias e='emacs'

# pass color through to less
alias less="less -R"

# make fewer mistakes
alias mv="mv -iv"
alias cp="cp -iv --reflink=auto"
alias ln="ln -iv"

# xinerama settings
dual () {
    xrandr --output eDP1 --primary --left-of HDMI1 --output HDMI1 --auto
}

mirror () {
    xrandr --output eDP1 --primary --same-as HDMI1 --output HDMI1 --auto
}

single () {
    xrandr --output HDMI1 --off
}

function ranger-cd {
    tempfile='/tmp/chosendir'
    /usr/bin/ranger --choosedir="$tempfile" "${@:-$(pwd)}"
    test -f "$tempfile" &&
    if [ "$(cat -- "$tempfile")" != "$(echo -n `pwd`)" ]; then
        cd -- "$(cat "$tempfile")"
    fi
    rm -f -- "$tempfile"
}
